#Ignore lines with comments in code
#(keyword), (action), (comment) format

(there), delete, (Drain the Swamp of Corrupt there's)
(their), replace with (trump), (Make Python Great Again)
(they're), flag, (They're is FAKE NEWS)
